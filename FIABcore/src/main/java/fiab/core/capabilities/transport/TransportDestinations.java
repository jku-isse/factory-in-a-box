package fiab.core.capabilities.transport;

public enum TransportDestinations {

    /**
     * The directions where the turning unit can face.
     * Use unknown only to instantiate a variable, but avoid using it
     */
        NORTH, EAST, SOUTH, WEST, UNKNOWN
}
