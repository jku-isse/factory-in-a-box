// Project configuration
brick.pref {
    // Main class - Edit this according to the machine this is running on //
    //mainClass = "TurnTableApplication"
    //mainClass = "motorSensorTest.MotorTestApplication"
    mainClass = "RobotTestApplication"
    // Brick connection parameters - The sshHost variable changes on almost every connect. Change it accordingly //
    sshHost = "10.78.115.170"
    sshUser = "robot"
    sshPassword = "maker"
    useSudo = true
}