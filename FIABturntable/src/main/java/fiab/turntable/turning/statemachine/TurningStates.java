package fiab.turntable.turning.statemachine;

/**
 * States according to the conveyor state machine specification
 */
public enum TurningStates {
    STOPPING, STOPPED, RESETTING, IDLE, STARTING, EXECUTING, COMPLETING, COMPLETE
}
